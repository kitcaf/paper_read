export function TasksPanel() {
  return (
    <article className="panel">
      <p className="eyebrow">Tasks</p>
      <h2>Background job progress belongs in its own module</h2>
      <p>
        The task center will subscribe to backend events and show screening and
        reading workflow progress.
      </p>
    </article>
  );
}
